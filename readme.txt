This file contain raw data in format.bib

a script (trasnfer_to_jason.py) as its name suggest, would help 
to convert to semi_structerd data.
 

After converting, a shell script (copy_json_files.sh) 
gother all .json files together.


we aime to get insight from the data.

